# RDV project working memory

## Core priorities and updates (2026-09-08)

- 2026-09-19 printer incident: user confirms actual missing paper and demands APP connection repair. Screenshot shows primary seven failures / >3000ms timeout, not merely unused backup. 1.1.3/code12 LIVE via b3946e3 (both Vercel projects successful; new live-status API and update API verified): XPYUN minimum10s and one same-key deduplicated retry; no unknown-outcome fallback/requeue; web/native receipt waitForResult with45s/no client replay; real cloud status; receipt-failure and kitchen-remote-ID audit. 144 web,62 JVM,debug/release lint/build,2 isolated API35 print flows passed. Signed APK/delta published and 2 signed-package API35 checks passed; deployment status in docs/release-1.1.3.md. No live printing/queue mutation; actual hotel paper acceptance remains open.

- 2026-09-10 final menu data adjustment applied directly and read back: 116 Crab（金玉蟹）inactive (not deleted, code retained); 059 Beef Skewer PHP 90; 046 Beef Roll renamed Beef Slice, still PHP 450; 067 Steamed Fish / 068 Hong Shao Yu / 071 Suan Cai Yu each PHP 1,600. All other rows/fields and all codes preserved by transactional before/after comparison. Script `scripts/menu/final-adjustments-2026-09-10.mjs`; ignored pre-change backup in `artifacts/menu-september-2026/`. Initial September export/registry/import are historical snapshots, now superseded for these entries: do not rerun the initial catalog refresh to undo this adjustment. No APK, application-code deployment, orders or printing touched.

- 2026-09-09 reported Android history/revenue rapid-filter slowdown: local maintenance fixes lazy native report rendering and serial/latest-only report reads on native/web, 20-second report deadline without automatic replay. Production read-only sample: 1,881 orders, year income/order SQL 8.463/32.509 ms; no live writes. 132 web + 61 JVM, debug lint/build and one 2,000-order API35 rapid-switch fixture passed. User explicitly authorized submitting, pushing and deploying both projects with full APK and in-app delta. Signed 1.1.2/code 11 LIVE via `fbd7aef`; both production checks successful. Release lint/build, exact delta reconstruction and two signed-package API35 checks passed. Public metadata/full APK/stable link/update API and downloaded-delta byte-exact reconstruction verified directly without proxy. Full 1,625,081 bytes; code-10 delta 1,278,175 bytes. Deployment evidence: `docs/release-1.1.2.md`; diagnosis: `docs/history-report-reliability.md`.

- 2026-09-09 UX batch 1.1.1 / code 10 is LIVE with in-app delta and full APK download. Final evidence: `docs/release-1.1.1.md`. User wants maintenance only afterward; do not initiate feature expansion or unsolicited monitoring. Approved A1–A5, A8–A10, B1–B2 plus quantity badge/plain add without cart auto-open; explicitly defer A6 reverse-checkout targeting and A7 discount/service-fee workflows. Local isolated manager preview `http://127.0.0.1:3106`; backend API traffic is in-memory fixture only. New guests/session filter/checkout quote APIs are additive and must accompany eventual rollout. 130 Web tests + build, 59 JVM + native lint/debug build, 3 isolated API35 flows passed; targeted UI refinements checked separately. Source `d348fe3` pushed; backend deployment READY. Signed 1.1.1 and code-9 delta staged with matching prior certificate; release lint/build, two signed-package API35 checks and exact reconstruction passed. Release `7442fa3` pushed after source readiness; both production deployments READY. Direct public full APK/delta SHA checks and byte-exact public delta reconstruction passed; update API returns 1.1.1/code 10. Full APK 1,625,081 bytes; code-9 delta 1,288,712 bytes. No live business writes. Details: `docs/pending-product-experience.md`.

- Combined 1.1.0 is LIVE: user authorized fast checks/fixes/publication of all post-1.0 changes, superseding source-only limits below. Source `c597db7`, signed release `0645b3e`; Android code 9, Android 8+. Public APK `distribution/site/public/releases/rdv-order-1.1.0.apk`, 1,608,565 bytes, SHA-256 `9c634275e7bd4998726d42eb38a96bdebaf8e926ce24d1d06950223412090992`. Both Vercel deployments successful; direct public APK checksum matches. Migration 024 and catalog applied, all 182 live entries match the exported registry/options/prices; original 152 preserved. Reused 118 menu/backend tests and previous menu/UI evidence; 14 focused web, 16 JVM, 3 API-35 device checks, two browser recovery sizes and production builds passed. Existing 1.0 clients need this full upgrade once to acquire delta support. Details/final local evidence: `docs/release-1.1.0.md`. No live order/printing/queue test performed; hotel device/printer acceptance remains on site.

- Pending menu/options/codes batch: user authorized implementation from three PDFs, preserving unmatched employee dishes. Explicit overrides: Americano 150 everywhere, Latte 180, Kung Pao Chicken 480; one Chop Suey with Chicken 420/Pork 450. Ten free breakfast sets require a zero-price concrete drink. Three-digit immutable global-search codes; code/name workbook has 182 dishes. Web/native/backend source and isolated fixtures implemented; production data only read, not changed. Migration 024, client upgrade and catalog activation must be coordinated; old native clients cannot select required options. Evidence and rollout: `docs/menu-september-2026.md`; export `outputs/menu-september-2026/RESORT-DEJA-VU-菜单编号.xlsx`. No commit/public APK/deployment this task; preserve concurrent updater/connectivity work.

- Settings home now uses one left-aligned menu entry per row with thin dividers and trailing chevrons on both web/native, replacing the 2×4 grid. Labels, order and role visibility are retained. User requested a logged-in local preview: web manager `/manage` at `http://127.0.0.1:3105` was shown in Chinese/English; native remains source-only. No tests/build/package/commit/publish in this pass. See `docs/design-guidelines.md`.

- Pending connection UX/reliability batch: user approved globally removing web/native network indicators and using a confirmed-failure modal with Retry, automatic foreground recovery and automatic dismissal. Header spacing adjusted; no header banner. Source implementation and boundaries: `docs/pending-connection-recovery.md`. Explicitly defer tests/typecheck/lint, builds, version bumps, commits and publishing until the combined batch. New `/api/connectivity` endpoint must precede the future APK; no released artifact changed.

- Pending user request after 1.0.0: implement optional APP delta updates while retaining full APKs on the download site; auto-check on entering Updates with disabled latest-version status; reduce menu/report repeated work. This round is explicitly source-only: no tests, commit, version bump, APK build or deployment. See `docs/pending-incremental-update.md`; delta support is unverified and must receive focused updater checks before the next batch release. Existing 1.0.0 clients need one full upgrade to acquire delta support.

- 1.0.0 milestone delivery: user explicitly requested removing the Settings instruction on both clients, reusing recent tests, committing, packaging and publishing the accumulated UI/performance batch. Web/native versions are 1.0.0; Android code 8. Signed public artifact `distribution/site/public/releases/rdv-order-1.0.0.apk`, 1,592,129 bytes, SHA-256 `bd9295dbfae3b631bbebd2dccd52be41eb005c0b87481132e62ab6c80b98c503`; certificate matches 0.1.6. Detailed bilingual notes cover ordering/management visuals, draggable settings/update management, simpler filters/menu entry and performance/reliability. Local type check, release lint/build and staging verification passed; previous 109 web / 48 JVM / 3 fixture device cases reused. Deployment status belongs in `docs/release-1.0.0.md`.
- Vercel now runs `npm run build`, not duplicate full tests after a locally verified batch. Do not repeat full web/JVM/device suites for copy removal, version bumps or documentation. Retain relevant artifact identity/signature/metadata checks.

- Latest explicit user direction: avoid excessive/repeated verification. Follow the proportional verification rules in `CONTRIBUTING.md`, which supersede older blanket per-commit/full-suite requirements below. Reuse unchanged passing baselines; docs-only edits do not trigger runtime tests/builds/device/live checks. After relevant checks pass, deliver without expanding the test scope merely for reassurance. Group a delivery into one push when practical.

- User priority: reliability/durability first, then speed and small/light implementation; keep UI attractive without unrelated redesign. Changes affecting web and native business behavior must be implemented and verified on both sides; web UI code does not automatically update native code. Menu/price data changes normally do not need an APK.
- User authorized complete in-app update implementation and delivery. Every newer published APK is mandatory, without skip. Check once per process cold start, not on resume or during ordering. Unknown version/check failure allows use; a confirmed newer requirement persists across restarts. Retain existing drafts, no new business/draft flows.
- Every published update must include a concise bilingual update detail on the mandatory-update page. Small changes use a broad one-line summary such as “修复已知问题，优化使用体验。/ Bug fixes and experience improvements.” Large releases may list specific additions. The staging command makes both messages required; do not omit them.
- The mandatory-update page follows the Android device system language for all copy and details. It must not show a language switch; the normal app's saved language selection does not affect this page.
- User release preference (2026-09-08): do not publish an APK for extremely minor isolated changes. Accumulate several related small fixes/improvements, then publish them together as one small improvement with a concise combined update detail. Publish immediately only for a material feature, reliability/security issue, or other concrete need to get a fix onto hotel devices.
- Native 0.1.3 / versionCode 4 adds this updater. Original 0.1.2 must be manually upgraded once. New signed APK: `distribution/site/public/releases/rdv-order-0.1.3.apk`, SHA-256 `f798cebfbd5b96b8c57aaf634c16e0ad2676dc3bea0751ed266557d39b1ed264`; 1,521,277 bytes. Preserve older APKs and signing key. Rules: `docs/android-in-app-updates.md`; final test/deployment evidence: `docs/android-progress.md`.
- `npm run android:stage-release` stages a built signed APK, validates version/signature/alignment, and updates public metadata/links together. It refuses overwriting versioned APKs. `ANDROID_SERIAL=<isolated-emulator> bash scripts/android/test-release.sh` runs real signed artifact checks using test-only assets in ignored build output.
- 0.1.3 is live: implementation `9458b59`, signed artifact `b664514`, both Vercel projects successful. Direct no-proxy APK SHA matches. API-35 emulator verified mandatory gate, download, denied permission retry, cancelled installer retry, actual system upgrade to code 4 and retained login. 44 JVM + 18 distinct device cases passed, plus 2 compact-landscape repeats; physical hotel device/printer acceptance remains open. Opt-in `UpdateLiveInstallTest` uses a private lower-version harness, never an invented public release.

- Brand naming standard: always write the hotel name as `RESORT DEJA VU` (no accented “Déjà”). Use the canonical starless `rdv-wordmark.svg` for rendered RDV wordmarks rather than a system typeface.

## Android migration (2026-09-07)

- The user authorized implementing a **native Kotlin / Jetpack Compose Android app**, producing an installable APK, adding meaningful tests and documentation, double-checking changes, and committing in separate scopes. The user handles on-site coordination.
- User clarification: this repository is the existing ordering software built specifically for their own hotel. Migrate this exact product for that one hotel. There is no multi-store product, tenant onboarding, store selection, or commercial distribution requirement. Refer to the hotel and its existing backend/devices; do not ask the user to decide a store/distribution strategy.
- Latest deployment state (2026-09-07): production is LIVE at https://order.resortdejavu.cn in the existing Vercel account. GitHub `zevs1120/rdv-order` main is linked; first automatic deployment of `9919baf` succeeded with 94 tests. Do not revert to the earlier not-deployed assumption. No platform change, paid upgrade, or always-on worker is required.
- User communication boundary: do not independently classify this project as commercial/non-commercial or turn unsolicited legal/platform-terms interpretations into engineering gates. The user handles project classification and their relationship with Vercel. If asked for such analysis, distinguish source information from interpretation and leave decisions to the user.
- Printing history evidence: `d89d3ec` awaited `runPrintWorker(1)` after order commit for serverless printing; `0b704d8` changed this to conditional background wake; `ee66a93` removed the wake call. These are local Git findings, not proof of which revision Vercel deployed. Reconcile this regression/version difference before changing architecture.
- Preserve the existing web application's major layout, interaction order, business rules, connected APIs, database and server-side printing. Do not substitute a WebView or redesign the product.
- The detailed plan is `docs/android-native-migration-plan.md`. Track concrete decisions and implementation evidence in `docs/android-progress.md`; distinguish local/mock verification from real deployment/printer verification.
- Baseline web commit: `ee66a93`. Some older docs/screenshots do not match current code. Current login sends managers to `/manage/orders`, waiters to `/tables`. `/manage/cashier` and `/manage/ops` redirect to `/manage/income`.
- Order POST now restores automatic printing through Next.js `after()` only for a new insertion, targeting that order with `runOrderPrintWorker(orderId)`. Old queued jobs must not steal its attempt. Same-key replay never schedules a second print or recreates a cleared job. The existing PRINT_WAKE_ON_ORDER flag, manual retry and clear-queue behavior remain. Accounting writes are unchanged.
- Use the stable production root URL above; resolve target Android devices from available context, asking only for information that cannot be established. APK installation for the user's hotel is the requested delivery; no separate store-distribution decision is needed. Do not invent connection details or claim physical printing is tested.
- Server/printing secrets must never be embedded in the APK or documentation. Build artifacts, local toolchains, signing stores and machine-specific configuration are ignored by Git.
- Before a commit follow `CONTRIBUTING.md`: run `npm run verify`; additionally run relevant Android build, unit tests and lint for Android changes. Reuse an unchanged passing web baseline across adjacent Android-only scopes; rerun if web source changes. Do not mark unperformed device/printing checks as passed.
- The latest user explicitly authorized handling GitHub pushes/branch integration, Vercel project creation/linking, production environment configuration and deployment end to end. This supersedes earlier deployment-preparation-only limits. Use isolated fixtures for automated business tests; do not drain or clear the existing historical print queue as part of deployment.
- Native client now lives in `android/`; `npm run android:verify` runs resource parity, JVM tests, lint and APK build. `ANDROID_SERIAL=<isolated-device> npm run android:verify -- --device` additionally runs device tests. Always specify a serial for installs/device commands; multiple AVDs may be connected.
- `npm run android:apk` exports signed debug `artifacts/android/rdv-order-0.1.1-internal.apk` plus SHA/signature/alignment/metadata. Internal package `com.rdv.order.test`, min API 26, target/compile 36. Production `com.rdv.order` now has a pinned production origin and private local signing configuration; release APK is `artifacts/android/release/rdv-order-0.1.1.apk`. Signing backup is in `/Users/qiao/Documents/rdv-order-signing`; never print/commit its contents or replace the key for updates.
- Current local evidence: web verify 94 tests + build (includes 8 PostgreSQL WASM order/print integration tests and 9 optional dispatcher tests); Android 33 JVM + 13 API-35 device tests, lint/build. See `docs/android-progress.md` for exact boundaries and `docs/android-acceptance.md` for remaining parity/CRUD/CSV/physical print/device acceptance. Do not call this production-ready or equate fixture callbacks with actual printing.
- Low-height native ordering content scrolls while cart/submit remain visible. Device screenshot tests must assert actual visibility and capture the whole display (dialogs use separate windows); a passing synthetic click alone previously missed clipped landscape controls.
- Native implementation commit `53c69fa`, recovery API `c1966d2`, plan baseline `b8c2885`. Final internal APK installed/cold-started on API 35; phone, 360dp small-screen/1.3 font and 640dp landscape screenshot flows passed. Curated fixture screenshots are in `docs/android-assets/`; exact APK checksum and release blockers in `docs/android-progress.md`.

- Native session/draft ownership fix commit `5de2d4d`; current internal APK is 0.1.1, versionCode 2. Expired sessions clear old UI data after saving drafts; queued saves capture their original account scope.
- Cloud preparation added opt-in `npm run worker:print` for the existing dispatch API; it has never been activated and is NOT a required part of the Vercel migration. Do not launch it against existing DB/printer settings without reviewing queued jobs and test authorization. It injects only the HTTPS root and worker key, runs one job at a time, and must be the sole automatic scheduler. It does not prove printer-side exactly-once delivery. See `docs/hotel-first-deployment.md` and `docs/printer-deploy.md`.

- Vercel preparation: `vercel.json` selects Next.js, installs `npm ci --include=dev`, and builds `npm run verify`; package engines select Node 22.x. Reviewed implementation has been fast-forwarded to and pushed on `main`; original scoped commits are preserved. Git CLI lacks a push credential and the GitHub connector rejects tree writes; GitHub Desktop is already authenticated and successfully pushes this repository. Use its UI for pushes unless CLI authentication is later fixed. Production branch pushes deploy production; other branches usually preview. Keep preview DB/printer credentials isolated.
- Deployment dependency maintenance: Next.js 15.5.25 (same major), Vitest 3.2.7, PostCSS override 8.5.28, refreshed sharp/nanoid lock versions. PGlite 0.5.8 is test-only; it exercises actual SQL/schema in memory and does not replace production Postgres or prove multi-connection/cloud/printer behavior.

## Completion reporting

Report APK location, build type, supported Android baseline, exact tests performed, remaining release blockers and commit IDs. An internal test APK is not an approved production replacement. Keep this file and progress documentation current as decisions arrive.

## Active deployment setup (2026-09-07)

- Vercel project `rdv-order` created in the existing `renfei-zhaos-projects` scope. Local `.vercel/` is ignored. All 15 required/current local DB/XPYUN settings are saved as production secret variables, not Preview. No credentials are recorded here.
- Read-only database checks succeeded: schema tables present, existing data retained. The DB hostname identifies ap-southeast-1, so function region is sin1 (Singapore). Historical pending queue is not touched.
- GitHub remote main is an ancestor of the reviewed implementation. Production branch is main; first automatic build/deployment succeeded. Preview deployments are disabled until isolated data/provider configuration exists.

- Live verification: 17 read-only HTTP checks passed, including manager/waiter login, menu/tables, management queries, permissions and print configuration (ready=true). Historical pending queue remains 108; no business writes or real paper tests were performed. Local direct DNS returned an unrelated address; checks use the existing local HTTPS CONNECT proxy, without disabling TLS validation. Hotel-network availability still needs on-device evidence. See `docs/deployment-delivery.md`.

## APK download distribution (2026-09-07)

- User authorized a public APK download site under their own `resortdejavu.cn` domain; selected `download.resortdejavu.cn`. DNS is managed through the already logged-in Tencent Cloud DNSPod account. Only add the download CNAME and its required verification TXT; preserve apex/www/mail and prior verification records.
- Static source is `distribution/site/`, deployed as a separate Vercel project `rdv-downloads` linked to this GitHub repository/main with that Root Directory. It has no database or printer credentials. The original `rdv-order` backend and embedded APK origin remain unchanged.
- Explicit exception to ignored build artifacts: the reviewed, signed public APK is versioned in `distribution/site/public/releases/` for repeatable Git-based downloads. Never commit keystores/properties, internal/debug APKs, credentials or runtime data. The download build verifies APK SHA-256, size, version and links against `release.json`; never overwrite an existing versioned APK.

## Login timeout correction (2026-09-07)

- User installed 0.1.1 but login timed out on their network. Old .vercel.app origin also times out in local direct checks. Backend is now bound to `https://order.resortdejavu.cn`, with DNSPod CNAME and ownership TXT; use it for all future release builds. Backend/database/printing routes are unchanged.
- Latest signed download is 0.1.2 / versionCode 3 at `distribution/site/public/releases/rdv-order-0.1.2.apk`, same release certificate. Download page and stable APK link now select 0.1.2. Keep 0.1.1 immutable as historical artifact, not the current recommendation.
- The new origin maps to the legacy origin only for local storage keys, preserving sessions, drafts, pending idempotency keys and menu cache across update. Transport uses the custom origin. Do not alias arbitrary domains or accounts. Three regression tests cover these boundaries.
- Login and live API checks must use a direct connection without proxy; a proxy-only pass is insufficient evidence for this reported failure. Current direct checks: waiter and manager login/tables, menu, auth rejection and print health passed. Physical hotel-device success still needs user confirmation after installing 0.1.2.

## Single-screen team app downloads (2026-09-07)

- Latest user direction: keep the download site to one screen, with a simple brand bar, capsule switch for Ordering app / Staff app, app name/icon, download button and version information. Do not reintroduce feature introductions, account/PIN/configuration explanations, platform disclaimers or installation steps.
- Staff app is still being made and has no uploaded release; show only “制作中 / In the making”, with no download or invented features. Preserve bilingual switching and existing RDV Order 0.1.2 APK, signature, links and backend.
- Visual references are the user's `rdv-website` and `Taboo` projects: real RDV logo, warm cream/deep green, serif title, subtle raised capsule surfaces, minimal hover motion. Sources and UI evidence are documented in `docs/design-guidelines.md` and `docs/app-downloads.md`.

## Backend-only maintenance (2026-09-19)

- User explicitly authorizes implementing the reviewed backend optimizations while preserving every existing layout, button, workflow and business result; no APP changes or packaging. Bill reads 4→2; receipt preparation 3→2 with identical content; menu reads 4→3/shared scan; checkout finance reads batched/skipped without taxes. Print claims atomic/single-task; database-save failures never resend accepted jobs, abandoned unknown tasks stop retries, stale threshold minimum120s. Main pool max6 unchanged, idle default30s; metadata-only max1 bounded pool. No schema migration, new scheduler or live queue mutation. 164 tests + TypeScript/production build and print-env presence check passed; unchanged Android artifacts/tests not rerun. LIVE via `77ede66`; both Vercel checks successful and backend dashboard Ready. Direct menu/tables/health/update reads returned200; menu SQL parity passed, public app remains1.1.3/code12. No open table for live bill read. Health was unknown on one server-side query; separate local read-only cloud query online (366ms), not paper acceptance. Scope/evidence/delivery: `docs/backend-maintenance-2026-09-19.md`.

## XPYUN integration incident direction (2026-09-20)

- Latest user explicitly rejects bypassing XPYUN, local bridges and additional hardware. Keep the existing XPYUN cloud network printer and audit/fix its official integration. The local-execution proposal in `docs/print-delivery-redesign-2026-09-20.md` is superseded and not authorized for implementation. Preserve all UI/workflows and current APK.
- User confirms no physical paper even for the 13:47–13:48 jobs that XPYUN queryOrderState labels printed; printer was not restarted and phone Wi-Fi is reported stable. Do not call this restored or repeat those onsite questions. Cloud acknowledgement and cloud-reported completion are not physical acceptance.
- Official Node SDK and current API documentation compared: JSON submission, SHA1 signature, endpoint and required parameters broadly match. Current error classification incorrectly treats 1006 (invalid order date) as transient and omits the documented one-time retry for 1004 (ADD_ORDER_FAILED). These are contract discrepancies, not established causes of the recorded transport timeouts. No runtime correction or deployment has been performed in this audit. Next evidence needed is platform order-to-device identity/delivery detail for the existing remote IDs; do not blindly switch nodes or replay historical jobs. Incident evidence: `docs/print-incident-2026-09-20.md`.

- Follow-up console evidence: sole KITCHEN device (SN suffix 014B, 58mm, firmware D81.14) matches local account/SN. Three 13:47–13:48 cloud orders target it; latest detail is TABLE 10 / PLAIN RICE PHP25, not screenshot TABLE06 / Rice Platter PHP150. One authorized plain-text official-API test sent 14:09:58, marker RDV-CLOUD-884598, remote OM26092014095885012205, HTTP200/code0 in338ms; queryOrderState true. Physical result pending; no retries or historical replay. Existing receipt has empty N tags, a documented possible 1004 trigger, not proven timeout/no-paper cause. No code/config/APK changes yet.

- Latest user confirms controlled plain-text test RDV-CLOUD-884598 did NOT print; stop repetitive on-site verification. User authorizes a full official XPYUN backend integration rewrite while preserving UI/APK/business flow. Implemented isolated adapter and canonical tags, strict return validation, official1004 bounded retry, no false1013 success, unknown-outcome preservation, no silent long-ticket truncation. No device rebinding/node switch/cloud queue reset. Evidence and delivery status: `docs/xpyun-integration-2026-09-20.md`. This does not prove the external cloud-to-device no-paper fault fixed.

- Official XPYUN rewrite LIVE via `7779d05`: both Vercel checks success, production menu/tables/print-health/app-release direct reads200, primary=xpyun ready=true, public code12 unchanged. TypeScript +186 distinct tests +production build +print-env passed (fixture-only failure rerun11; reused185 passes). No new physical test, no queue mutation, no APP build. User-confirmed no-paper fault is NOT physically verified resolved. Deployment details in docs/xpyun-integration-2026-09-20.md.


## Two-copy correction (2026-09-21)

- Latest user confirms kitchen can print after restart; missing front-desk copy and false failure/timeout remain. Explicitly restore kitchen + guest/front-desk copies per order; supersedes historical kitchen-only policy. Current implementation joins both contents in one XPYUN job, copies=1, preserves guest prices and current full-table Print Receipt. Reverts 7779d05's treatment of1013 as device failure; dedup acknowledgement does not certify physical delivery. Bounded queryOrderState confirmation runs after phone response/insideworker and logs print.delivery; no confirmation retry can resend paper. No queue clears, schema change or live test printing.
- Native submit's post-success bill read uses generic fail; initial order timeout only gets request-status on next manual retry. Complete client correction needs APK. Asked user whether to lift prior no-APP boundary; no answer yet, do not package without resolution. Delivery: docs/print-two-copies-2026-09-21.md. Preserve unrelated Staff download changes.

- User explicitly approved APP release with unchanged layout. 1.1.4/code13 fixes web/native initial ambiguous submit through original-key read-only recovery; saved-order bill refresh errors are distinguished. 195Web/65JVM/release lint+build/3API35 order flows/2signed-package checks passed. Signed APK and code12 delta staged; docs/release-1.1.4.md tracks delivery. This supersedes the unanswered/no-package line above.

- 1.1.4/code13 LIVE via `8815f12`: both Vercel deployments successful. Direct public metadata/update API, full/stable APK SHA and downloaded delta with public-base exact reconstruction passed. Original signer, Android8+, 1,625,429-byte full APK; layout unchanged. Unrelated Staff changes excluded. No live printing/queue mutation; physical two-copy acceptance remains unverified. Final evidence: docs/release-1.1.4.md.

## Printing rebuild (2026-09-23)

- User authorizes replacing the entire print subsystem, prioritizing actual fast/simple printing and brief-disconnection recovery; explicitly requests only minimal focused checks before their real hotel trial. Same physical printer prints kitchen + priced guest copies. Short outages auto-recover; old backlog is manual. Do not turn internal delivery states into elaborate staff prompts.
- New independent `lib/printing/` replaces old print.ts/worker/XPYUN client/confirmation/fallback/dispatcher. New immutable print_deliveries (migration026 applied, historical print_jobs untouched). Vercel Workflow durably starts before transaction commit; after is optional fast wake. Official XPYUN mode1 buffers until creation+120s; fixed same-key bounded retry, known remote IDs query only. Web/native receipt intents persist per account+table session; UI layout retained.
- Signed 1.2.0/code14 built/staged, original signer, Android8+, full1,625,433 bytes and code13delta1,282,709. Focused checks: queue6, transport3, order/receipt9, client14, JVM16, TypeScript/Next build, release assemble/lintVital, artifact identity/signature/delta/metadata. Broad suites/device flows not rerun per latest direction. LIVE via9d8e3cd: both Vercel checks success, direct public metadata/update/full+delta SHA/newstatus API passed. Cloud-reported printer online348ms, newqueue0/0; not paper proof. See docs/release-1.2.0.md. No live printing, historic queue replay/clear, device rebind or node switch; actual paper remains for user trial.
