# 下一批待发布改动 · 2026-09-08

> Superseded delivery boundary: user authorized combined verification and release as 1.1.0 on 2026-09-08. See `release-1.1.0.md` for reused/new evidence and current rollout; source-only statements below describe the earlier implementation stage.

用户明确要求本轮仅快速改源码，不测试、不提交、不打包、不升版本、不发布。线上 RDV Order 1.0.0 不变；本记录不能作为已验证证据。

- APP 差量下载和重建、发布脚本及下载站补丁校验已接入源码，边界见 `android-in-app-updates.md`。全新下载仍为完整安装包。
- 原生更新管理进入时自动检查；最新版状态按钮禁用。未知状态不伪装成功，发现新版本仍强制升级。网页无法知道手机安装了什么版本，因此网页按钮使用“已获取最新发布版本”，不声称手机已是最新版。
- 网页菜单按分类预建索引，菜名/价格展示按菜单与语言预计算；切换目录复用数据，不再用占位遮住已有菜品。移除切换大类时把上一类菜品写进新类缓存的副作用；切换时重置列表滚动位置，避免沿用旧虚拟行号出现空白。
- 原生菜单增加最多 32 项、按后台来源隔离的内存缓存，60 秒内切回已读取的大类直接使用；手动刷新始终请求服务器，菜单管理写入清除内存缓存。既有离线缓存/订单提交校验不变；重复选择同一子类不重复保存草稿。
- 网页订单/收入取消已过时和离页请求，并阻止旧查询覆盖新日期结果；原生复用已解析报表行、总额和日期显示。两端财务仍取实时数据，不用旧收入数字冒充即时查询，没有承诺网络延时为零。

仅做源码阅读和编辑，没有运行类型检查、测试、构建、设备或线上验证。下批针对更新器与请求切换补检查，复用无关既有基线。保留其他任务对 Staff 下载配置、菜单文档等文件的改动。
